const assert = require('assert');
const randomQuotes = require('../index.js');

describe('randomQuotes', function () {
  it('has a test', function () {
    assert(false, 'randomQuotes should have a test');
  });
});
